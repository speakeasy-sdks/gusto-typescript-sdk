import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";

export enum CustomFieldTypeEnum {
    Text = "text",
    Currency = "currency",
    Number = "number",
    Date = "date",
    Radio = "radio"
}